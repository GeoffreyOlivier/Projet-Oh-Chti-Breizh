{% extends 'base.html.twig.' %}

{% block title  %}Mes Commandes{% endblock %}

{% block main %}
    <div class="container mt-3">

        <h2>Ma commande</h2>
        <div class="col-md-10">
            {% for type, flash_messages in app.session.flashBag.all %}
                {% for msg in flash_messages %}
                    <div class="alert alert-{{ type }}">
                        {{ msg }}
                    </div>
                {% endfor %}
            {% endfor %}
            <table class="table table-striped table-hover" width="100%">
                <thead>
                <tr>
                    <th>N° Commande</th>
                    <th>Références</th>
                    <th>Nom</th>
                    <th></th>
                    <th>Quantité</th>
                    <th>PrixUT</th>
                    <th>Total</th>
                </tr>
                </thead>
                <tbody>
                {% if  commande | length == 0  %}
                    <tr>
                        <td colspan="6" class="text-center">Pas de commande en cours</td>
                    </tr>
                {% endif %}
                {% set somme = 0 %}
                {% for key, product in commande %}
                {%for prod in product%}
                    <td> {{prod.id}} {{prod.nom}} {{prod.quantite}}</td>
                    {% endfor %}


{#                        <tr>#}
{#                            <td>{{ commande.id }}</td>#}
{#                            <td>{{ commande.panier[commande] }}</td>#}
{#                        </tr>#}
{#                            <td>{{ product.nom }}</td>#}
{#                            <td>#}
{#                                <img  class="image-style-thumb" src="{{ asset("images/") ~ product.nom }}.jpg" height="40" alt="{{ product.nom }}" />#}
{#                            </td>#}
{#                            <td>{{ product.prix }} €</td>#}
{#                            <td>{{ (product.prix * panier[product.id]) }} €</td>#}
{#                        </tr>#}
{#                        {% set somme = somme + product.prix * panier[product.id] %}#}
{#                    </form>#}
                {% endfor %}
{#                </tbody>#}
{#                <tfoot>#}
{#                <tr>#}
{#                    <th colspan="5" class="text-right">Total Commande:</th>#}
{#                    <th> {{ somme }} €</th>#}
{#                </tr>#}
{#                </tfoot>#}

{#            </table>#}
{#            <p class="card-text">#}
{#                <a class="btn btn-primary" role="button"  href="{{ path('panier.supprime') }}">Vider le panier</a>#}
{#                <a class="btn btn-primary" role="button"  href="{{ path('panier.enregistrement') }}">Valider</a>#}
{#            </p>#}
{#        </div>#}
{#        <div class="col-md-4"></div>#}
{#    </div>#}

{% endblock %}





